# solo repo for nix and home-manager specific configuration. WIP

### bootstrap:
1. git clone it to /s
2. ln -sf /s/hosts/siMONSTER.nix /etc/nixos/configuration.nix

todo:
* proper readme
* add two more hosts
* come up with more refined structure
* finish this se...
